import type { Viewport } from '../types/checks';

export const VIEWPORT_WIDTHS: Record<Viewport, number> = {
  desktop: 1280,
  mobile:  320,
};

export const VIEWPORT_HEIGHTS: Record<Viewport, number> = {
  desktop: 800,
  mobile:  568,
};
