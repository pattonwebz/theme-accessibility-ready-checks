import { test } from '../helpers/fixtures';
// import { recordResult } from '../helpers/result-collector';
// Uncomment above and implement when writing the real test

/**
 * Check 11: Alt Text
 * Check ID: alt-text-1
 * Templates: all 8
 * Viewports: desktop
 * Tool: axe-core (image-alt, role-img-alt rules) + custom rule for suspicious patterns
 * 
 * Tests that all images have appropriate alt text; no generic/suspicious alt values.
 * Note: Custom suspicious-alt rule to be implemented separately.
 */
test.describe('check-11: alt text', () => {
  test.todo('alt-text-1 — verify all images have appropriate alt text (all templates)');
  test.todo('alt-text-1 — verify no generic/suspicious alt text patterns (all templates)');
});
